<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>入力フォーム</title>
    <style>
    .centered-container {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}

form {
    position: relative; /* 追加 */
    width: 80%;
    max-width: 600px;
    border: 1px solid black;
    background-color: #FFE4B5;
    padding: 20px;
    text-align: center;
}

.input-group {
    margin-bottom: 20px;
    font-size: 25px;
    display: flex;
    flex-direction: column;
    align-items: center; /* この行を追加 */
}

.input-group label {
    width: 100%;
    margin-bottom: 10px;
    text-align: center; /* この行を追加 */
}

input[type="submit"] {
    font-size: 25px;
}

input[type="submit"] {
    background-color: #007BFF; /* 青色を背景色として設定 */
    color: white; /* テキストの色を白にするためのオプション。青色の背景に適しています。 */
    border: none; /* ボタンの境界線を削除 */
     font-size: 25px;
    padding: 10px 20px; /* ボタンの内部の間隔を設定。調整が必要な場合は変更してください。 */
    cursor: pointer; /* ボタンのカーソルスタイルをポインタに変更 */
}

/* ボタンがホバーされたときのスタイルを追加するためのオプション */
input[type="submit"]:hover {
    background-color: #0056b3; /* ホバー時の色。少し暗めの青色にしています。 */
}

</style>
</head>

<body>
<div class="users-form">
  <div class="centered-container">
        <form action="your-server-endpoint" method="post">
        <span id="closeButton">×</span>
        <h1 id="currentDate"></h1>
            <div class="input-group">
                <label for="bodyFatPercentage">体脂肪率(%):</label>
                <input type="number" id="bodyFatPercentage" name="bodyFatPercentage" step="0.1" placeholder="体脂肪率を入力">
            </div>
            <div class="input-group">
                <label for="weight">体重(kg):</label>
                <input type="number" id="weight" name="weight" step="0.1" placeholder="体重を入力">
            </div>
            <div class="input-group">
                <label for="muscleMass">筋肉量(kg):</label>
                <input type="number" id="muscleMass" name="muscleMass" step="0.1" placeholder="筋肉量を入力">
            </div>
            <div>
                <input type="submit" value="記録する">
            </div>
        </form>
  </div>
</div>
    
    <script>
        document.addEventListener("DOMContentLoaded", function() {
            // 今日の日付を取得
            let today = new Date();
            let formattedDate = `${today.getFullYear()}年${today.getMonth() + 1}月${today.getDate()}日`;

            // 日付をヘッダーにセット
            document.getElementById("currentDate").textContent = formattedDate;
        
           let closeButton = document.getElementById("closeButton");
closeButton.addEventListener("click", function() {
    document.querySelector("form").style.display = "none"; // ".centered-container"から"form"に変更
});
});

    </script>
</body>

</html>
